-- Trade journal schema — stubbed out, fills in as I build in public.
-- This is where the tables go: sessions, campaigns (flat-to-flat sequences),
-- trade fills, and timestamped notes. Keep aligned with the backtest output
-- schema so live trades and simulated trades compare on the same axes.

-- Stub: schema lands as the journal / performance-analytics role gets built on camera.
