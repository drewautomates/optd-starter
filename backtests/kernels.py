"""Validated exit kernels.

Stubbed out — fills in as I build the desk in public. This is where the
bias-checked exit logic lives (entry -> stop/target -> exit price + realized R),
plus a validate_kernels() self-test that runs before any sweep. The Pine
reference in indicators/pine_reference/ is the spec; keep this in parity with it.

Ships with the deep-backtest video — subscribe at
onepersontradedesk.com/subscribe to get it first.
"""

# Stub: exit kernels + validate_kernels() land with the deep-backtest video.
